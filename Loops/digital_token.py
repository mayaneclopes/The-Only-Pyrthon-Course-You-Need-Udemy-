# A tea stall owner has a digital token display.
# For every customer in line, a loken number is printed and chai is served.
# Tasks:
# 1. Use a for loop to generate token numbers from 1 to 10 usen range()
# 2. Print: Serving chai to Token #[number]

for token in range(1, 11):
    print(f"Serving chai to token #{token}")